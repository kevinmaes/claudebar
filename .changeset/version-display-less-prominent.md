---
"claudebar": patch
---

Move version display to end of status line and only show when update available. Version info now displays as `↑ vX.X.X` only when a newer version is detected, reducing visual clutter.
